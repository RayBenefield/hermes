import describe from 'tape-bdd';
import domain from 'src/domain'; // eslint-disable-line

describe('Welcome', (it) => {
    it('adds a user if they don\'t exist', assert =>
        assert.deepEqual(true, true)
    );
});
